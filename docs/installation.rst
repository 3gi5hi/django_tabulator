============
Installation
============

At the command line::

    $ easy_install django_tabulator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_tabulator
    $ pip install django_tabulator
